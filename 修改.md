你现在在 Windows 本地仓库 D:\AHEU\GP\MatReflect_NN 中工作。

目标：重新落实以下改动，但必须严格避免误删正式数据和模型源码。先检查当前仓库状态，再实施修改。代码为准，旧文档不可信。

硬性约束：

1. 先执行 git status 和目录盘点，再开始改。
2. 任何删除、移动、批量清空操作之前，先列出“将被影响的具体路径”和原因；如果涉及数据目录，先暂停并等我确认。
3. 严禁盲目清空以下目录或其内容：
   - data/inputs/binary
   - scene/assets
   - references
   - models 下的模型源码
   - backend/runtime/system_settings.json
4. 可以修改代码、脚本、文档、配置；但不要擅自删除正式输入资产。
5. 优先使用 pathlib.Path；保持 Windows 兼容。
6. 修改后必须做最小验证：
   - python -c "import backend.main"
   - frontend\node_modules\.bin\tsc.cmd --noEmit -p frontend
7. 最终给我：
   - 改了哪些文件
   - 还剩哪些风险
   - 跑了哪些验证

本次要重新落实的内容：

一、删除桌面模式

- 删除 desktop/ 及相关桌面启动/打包脚本
- README 和 AGENTS 不再把桌面模式当正式链路

二、目录治理

- 正式场景主源为 scene/assets/
- 每个场景一个目录，主 XML 文件统一为 scene.xml
- references/ 为只读参考区
- data/ 为正式数据主目录
- backend/runtime/ 为正式运行时目录
- 不再把 scene/dj_xml 或根目录 dj_xml 当正式主源
- 删除 scene/batch_render_tool.py
- 根目录 torchmeta/ 若仍存在，则移除其“正式项目结构”地位

三、模型目录重组

- 将根目录模型仓库统一收口到 models/
- 使用：
  - models/Neural-BRDF
  - models/HyperBRDF
  - models/Connecting_measured_brdfs_to_analytic
- 主仓库不跟踪 models/\* 内容
- 不使用 git submodule

四、文档同步

- README.md
- AGENTS.md
- docs/

文档口径必须统一：

- 当前项目不再保留桌面模式
- 正式场景主源是 scene/assets/
- references/ 不参与正式运行链路
- backend/runtime/render_xml/ 是正式临时 XML 目录
- models/ 是本地模型池
- backend/config/model_registry.json 是中央模型配置
- 同适配器新模型优先改配置；新增适配器才改代码
